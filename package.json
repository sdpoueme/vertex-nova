{
  "name": "vertex-nova",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/home-agent.js",
    "dev": "node --watch src/home-agent.js",
    "dev:debug": "LOG_LEVEL=debug node --watch src/home-agent.js",
    "sonos:auth": "node scripts/sonos-auth.js",
    "build:web": "cd web && npm run build"
  },
  "dependencies": {
    "@strands-agents/sdk": "^1.0.0-rc.3",
    "nodemailer": "^8.0.5",
    "telegraf": "^4.16.3"
  }
}
