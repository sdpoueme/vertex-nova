# Vertex Nova — Device Alert Rules
# Devices are identified by their friendly name (from Alexa, email, or webhook).
# The agent auto-discovers devices via Alexa API every 6 hours.

settings:
  vocal_alerts: false
  poll_interval_seconds: 60

rules: []
